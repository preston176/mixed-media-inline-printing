<UserControl x:Class="MixedMediaPrint.App.Views.RunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="560">
    <StackPanel Margin="16">
        <TextBlock Text="4. Run" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

        <TextBlock FontSize="14" Margin="0,0,0,16">
            <Run Text="{Binding TotalPages, Mode=OneWay}"/><Run Text=" total page(s): "/>
            <Run Text="{Binding BodyPageCount, Mode=OneWay}"/><Run Text=" body, "/>
            <Run Text="{Binding TabPageCount, Mode=OneWay}"/><Run Text=" tab."/>
        </TextBlock>

        <TextBlock Text="Run mode" FontWeight="Bold" Margin="0,0,0,4"/>
        <RadioButton Content="Preview -- just show the page order, touches nothing" IsChecked="{Binding IsPreviewMode}" Margin="0,2"/>
        <RadioButton Content="Dry-run to file -- real GDI/DEVMODE pipeline, no paper, no admin required" IsChecked="{Binding IsDryRunMode}" Margin="0,2"/>
        <RadioButton Content="Physical -- sends the job to the real device" IsChecked="{Binding IsPhysicalMode}" Margin="0,2"/>

        <StackPanel Visibility="{Binding IsDryRunMode, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,12,0,0">
            <TextBlock Text="Output file:"/>
            <DockPanel Margin="0,4,0,0">
                <Button DockPanel.Dock="Right" Content="Browse..." Command="{Binding BrowseOutputFileCommand}" Margin="8,0,0,0" Padding="10,4"/>
                <TextBox Text="{Binding OutputFilePath, UpdateSourceTrigger=PropertyChanged}"/>
            </DockPanel>
        </StackPanel>

        <StackPanel Visibility="{Binding IsPhysicalMode, Converter={StaticResource BoolToVisibilityConverter}}" Margin="0,12,0,0">
            <TextBlock Text="This sends real pages to the physical device. Type PRINT to confirm:" Foreground="DarkRed" FontWeight="Bold" TextWrapping="Wrap"/>
            <TextBox Text="{Binding ConfirmationText, UpdateSourceTrigger=PropertyChanged}" Margin="0,4,0,0"/>
        </StackPanel>

        <Button Content="Run" Command="{Binding RunCommand}" Margin="0,20,0,0" Padding="16,8" FontWeight="Bold" HorizontalAlignment="Left"/>

        <TextBlock Text="{Binding ResultText}" TextWrapping="Wrap" Margin="0,16,0,0" FontFamily="Consolas"/>

        <Button Content="&lt;- Back" Command="{Binding BackCommand}" Margin="0,20,0,0" Padding="10,6" HorizontalAlignment="Left"/>
    </StackPanel>
</UserControl>
