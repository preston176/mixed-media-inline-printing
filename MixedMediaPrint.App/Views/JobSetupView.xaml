<UserControl x:Class="MixedMediaPrint.App.Views.JobSetupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="820" d:DesignHeight="620">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <TextBlock Text="3. Job Setup" FontSize="20" FontWeight="Bold" Margin="0,0,0,12"/>

            <DockPanel Margin="0,0,0,4">
                <Button DockPanel.Dock="Left" Content="Load body PDF..." Command="{Binding LoadPdfCommand}" Padding="10,4" Margin="0,0,8,0"/>
                <TextBlock VerticalAlignment="Center">
                    <Run Text="{Binding PdfPath, Mode=OneWay}"/>
                    <Run Text=" ("/><Run Text="{Binding PdfPageCount, Mode=OneWay}"/><Run Text=" page(s))"/>
                </TextBlock>
            </DockPanel>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap"
                       Visibility="{Binding ErrorMessage, Converter={StaticResource NullToCollapsedConverter}}" Margin="0,0,0,8"/>

            <Grid Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <GroupBox Grid.Column="0" Header="Add body page range" Padding="8">
                    <StackPanel>
                        <TextBlock Text="First page (1-based):" Margin="0,4,0,2"/>
                        <TextBox Text="{Binding NewBodyFirstPage, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Page count:" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding NewBodyPageCount, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Add body range" Command="{Binding AddBodyRangeCommand}" Margin="0,12,0,0" Padding="10,4" HorizontalAlignment="Left"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Grid.Column="2" Header="Add tab run" Padding="8">
                    <StackPanel>
                        <TextBlock Text="First tab number:" Margin="0,4,0,2"/>
                        <TextBox Text="{Binding NewTabFirstNumber, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Count (consecutive tabs):" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding NewTabCount, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Copies per tab:" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding NewTabCopies, UpdateSourceTrigger=PropertyChanged}"/>
                        <TextBlock Text="Custom label (optional, blank = bare number):" Margin="0,8,0,2"/>
                        <TextBox Text="{Binding NewTabLabel, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Add tab run" Command="{Binding AddTabRunCommand}" Margin="0,12,0,0" Padding="10,4" HorizontalAlignment="Left"/>
                    </StackPanel>
                </GroupBox>
            </Grid>

            <TextBlock Text="Job plan (in print order)" FontWeight="Bold" Margin="0,16,0,4"/>
            <ListBox ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem}" Height="200">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <Button Content="^" Width="24" Command="{Binding DataContext.MoveUpCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"/>
                                <Button Content="v" Width="24" Margin="4,0,0,0" Command="{Binding DataContext.MoveDownCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"/>
                                <Button Content="Remove" Margin="4,0,0,0" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ListBox}}" CommandParameter="{Binding}"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Description}" VerticalAlignment="Center"/>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Right">
                <Button Content="&lt;- Back" Command="{Binding BackCommand}" Padding="10,6" Margin="0,0,8,0"/>
                <Button Content="Continue to Run ->" Command="{Binding ContinueCommand}" Padding="14,6" FontWeight="Bold"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
